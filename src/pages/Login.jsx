// src/pages/Login.jsx

import React from 'react';

const Login = () => {
    return (
        <div className="home-page" style={{minHeight: 'calc(100vh - 80px)'}}>
            <h1 style={{color: 'var(--color-acid-purple)'}}>Authentication Terminal</h1>
            <p style={{fontSize: '1.2rem'}}>
                **Auth to be added**
            </p>
            <p style={{color: 'var(--color-text-dim)'}}>
                Please stand by for security protocol deployment (next week's task).
            </p>
        </div>
    );
};

export default Login;